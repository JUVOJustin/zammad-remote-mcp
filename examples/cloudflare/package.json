{
  "name": "zammad-remote-mcp-cloudflare",
  "version": "1.0.0",
  "private": true,
  "description": "Deployable Cloudflare Workers host for zammad-remote-mcp.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "wrangler deploy --dry-run --outdir .wrangler/dry-run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "zammad-remote-mcp": "^1.0.3"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "wrangler": "^4.0.0"
  }
}
