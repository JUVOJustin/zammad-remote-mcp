{
  "name": "@zammad-mcp/cloudflare",
  "version": "1.0.0",
  "description": "Cloudflare Workers host for the Zammad remote MCP server.",
  "license": "MIT",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "wrangler deploy --dry-run --outdir dist"
  },
  "dependencies": {
    "@zammad-mcp/core": "1.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250109.0",
    "wrangler": "^4.0.0"
  }
}
